'use client';

const Error = () => {
  return (
    <div className="w-full overflow-hidden h-screen flex justify-center items-center">
      <img src="/500.png" />
    </div>
  );
};

export default Error;
